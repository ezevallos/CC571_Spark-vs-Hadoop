# Seguimiento de Pruebas CountSales

## Preparación

Se crearon los archivos `java` del paquete CountSales (*SalesMapper.java*, *SalesCountryReducer.java* y *SalesCountryDriver*) que son los que nos ayudarán en el análisis de la pequeña base de datos `SalesJan2009.csv` el cual contiene 1000 filas de datos.

Se procedió a generar el `JAR`del proyecto para su uso en el terminal.

Al igual que se trabajo con el `countword`, esta parte realizará practicamente los mismos pasos, siendo estos:

* **Paso 1**.

  Crear el directorio de ingreso del archivo, con el comando:
  
* **Paso 2**.

  Se envía una copia del archivo `SalesJan2009.csv` al directorio de ingreso, mediante:

* **Paso 3** *(Opcional)*.

  Se verifica el archivo dentro del directorio de ingreso:
  
  También podríamos realizar un muestreo del contenido del mismo:

* **Paso 4**.

  Procedemos a realizar la ejecución del `JAR` CountSales para la aplicación del análisis establecido:

* **Paso 5**.

  Verificamos el resultado obtenido mediante:
